# limine.conf — Spinor OS ブート設定 (Issue #73)
#
# 注: Limine v8 以降、設定ファイル名は limine.cfg から limine.conf に
#     公式に変更された (構文も刷新)。本プロジェクトは v8.x-binary を使用。

# メニューで待たず即起動
timeout: 0

# シリアルにもブートローダのメッセージを出す
serial: yes

/Linear Spinor OS
    protocol: limine
    kernel_path: boot():/boot/spinor-kernel
